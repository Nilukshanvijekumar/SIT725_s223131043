# Applied Software Engineering - Group Project (SIT725)

Main Dockerized submission is in `volunteerhub/`.

## Marker quick start

From repository root:

```bash
cd volunteerhub
docker compose up --build
```

Main run/setup details are documented in:

- `volunteerhub/README.md`

Required verification endpoint:

- `http://localhost:5000/api/student`


